<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Medical & Health</title>
  
  <!-- Favicon -->
  <link rel="icon" type="image/png" href="/img/medical-logo.png">

  <!-- Meta Tags -->
  <meta name="title" content="Medical & Health">
  <meta name="description" content="A new approach to learning about medicine, medical surgery, health information in Kurdish">
  <meta property="og:type" content="website">
  <meta property="og:url" content="https://medicalhealths.org/">
  <meta property="og:title" content="Medical & Health">
  <meta property="og:description" content="A new approach to learning about medicine, medical surgery, health information in Kurdish">
  <meta property="og:image" content="/img/medical-logo.png">
  <meta property="twitter:card" content="summary_large_image">
  <meta property="twitter:url" content="https://medicalhealths.org/">
  <meta property="twitter:title" content="Medical & Health">
  <meta property="twitter:description" content="A new approach to learning about medicine, medical surgery, health information in Kurdish">
  <meta property="twitter:image" content="/img/medical-logo.png">
  <meta name="robots" content="index, follow">
  <meta name="referrer" content="no-referrer">
  <meta http-equiv="cache-control" content="public, max-age=31536000, immutable">
  <!-- <meta name="apple-mobile-web-app-capable" content="yes"> -->

  <!-- Schema.org Markup -->
  <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "Website",
      "url": "https://medicalhealths.org/",
      "name": "Medical & Health",
      "description": "A new approach to learning about medicine, medical surgery, and health information for Kurdish",
     
    }
   
  </script>
  <!-- "sameAs": [
  "https://twitter.com/yourprofile",
  "https://facebook.com/yourprofile"
] -->
  
  <!-- External Styles and Scripts -->
  <%- include('./links.ejs')%>
  <style>
    @keyframes pulse {
      0%, 100% {
        transform: scale(0.8);
        opacity: 0.7;
      }
      50% {
        transform: scale(1.2);
        opacity: 1;
      }
    }
    .animate-pulse-custom {
      animation: pulse 1.5s ease-in-out infinite;
    }
    body.loading {
      overflow: hidden;
    }
  </style>
</head>

 <body>
   <!-- <div id="loading-spinner-container" class="loading-indicator fixed inset-0 bg-white bg-opacity-80 z-50 hidden items-center justify-center">
  <div class="relative w-24 h-24 sm:w-32 sm:h-32 md:w-40 md:h-40 flex items-center justify-center">
    <div class="w-full h-full rounded-full"></div>
    <img src="/img/medical-logo.png" alt=" Logo" class="w-full h-full animate-pulse-custom rounded-full transform -translate-x-1/2 -translate-y-1/2 object-contain">
  </div>
</div>
  
  <script>
    document.addEventListener("DOMContentLoaded", function () {
  const spinnerContainer = document.getElementById("loading-spinner-container");
  const mainElement = document.getElementById("main-content"); // Target the "main" element

  // Show spinner and dim effect on the main element
  function showSpinner() {
    spinnerContainer.classList.remove("hidden");
    spinnerContainer.classList.add("flex");
    mainElement.classList.add("opacity-50"); // Dim effect
  }

  // Hide spinner and remove dim effect from the main element
  function hideSpinner() {
    spinnerContainer.classList.remove("flex");
    spinnerContainer.classList.add("hidden");
    mainElement.classList.remove("opacity-50");
  }

  // Simulate a delay to test loading (remove in production)
  setTimeout(hideSpinner, 2000);

  // Handle HTMX events for the main element only
  document.body.addEventListener("htmx:beforeRequest", function (event) {
    if (event.detail.target.id === "main-content") {
      showSpinner();
    }
  });

  document.body.addEventListener("htmx:afterRequest", function (event) {
    if (event.detail.target.id === "main-content") {
      hideSpinner();
    }
  });
});
    
  </script> -->